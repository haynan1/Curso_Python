# Aula 69 - VALIDAÇÃO DE CPF COM TRATAMENTO DE ENTRADA

Fonte Python editável: `../69 - aula63.py`

Bloco: **Projeto CPF**

## Navegação

- Aula anterior: [VALIDAÇÃO DE CPF (CÁLCULO DOS DÍGITOS)](68 - aula62.2.md)

## Ideia central

Limpa → Valida sequência → Calcula → Compara

## Conteúdo estruturado

### Explicação

Agora você evoluiu o código adicionando tratamento de entrada — isso é MUITO importante.

Vamos entender os novos pontos:

----------------------------------------
🔹 LIMPEZA DO CPF (REGEX)
----------------------------------------

Você usou:

re.sub(r'[^0-9]', '', entrada)

Isso significa:
- Remove tudo que NÃO for número
- Deixa apenas os dígitos

Exemplo:
'746.824.890-70' → '74682489070'

----------------------------------------
🔹 VALIDAÇÃO DE SEQUÊNCIA
----------------------------------------

entrada == entrada[0] * len(entrada)

Isso verifica se todos os caracteres são iguais:

Ex:
'11111111111' → inválido
'00000000000' → inválido

⚠️ OBS:
O ideal seria validar usando o CPF LIMPO, não a entrada original.

----------------------------------------
🔹 CÁLCULO DOS DÍGITOS
----------------------------------------

Você aplicou corretamente:

✔ Primeiro dígito (peso 10 → 2)
✔ Segundo dígito (peso 11 → 2)
✔ Regra do resto da divisão por 11

----------------------------------------
🔹 VALIDAÇÃO FINAL
----------------------------------------

Compara:

cpf_digitado == cpf_calculado

Se forem iguais → CPF válido

----------------------------------------
🔹 POSSÍVEL MELHORIA
----------------------------------------

Trocar:

entrada == entrada[0] * len(entrada)

Por:

cpf_enviado_usuario == cpf_enviado_usuario[0] * len(cpf_enviado_usuario)

Assim você valida corretamente após limpar.

### Divisões detectadas no arquivo

- FRASE MNEMÔNICA
- EXPLICAÇÃO DIDÁTICA
- EXERCÍCIO RESOLVIDO
- EXERCÍCIO 1 - FÁCIL
- EXERCÍCIO 2 - MÉDIO
- EXERCÍCIO 3 - DIFÍCIL
- EXERCÍCIO 4 - DIFÍCIL
- GABARITO
- Exercício 1 - Fácil
- Exercício 2 - Médio
- Exercício 3 - Difícil
- Exercício 4 - Difícil

## Como estudar esta aula

1. Leia a explicação.
2. Abra o arquivo Python fonte.
3. Execute o código.
4. Altere pequenos trechos.
5. Resolva os exercícios presentes no arquivo.
6. Registre dúvidas no Obsidian.

## Código fonte completo

````python
"""
Calculo do segundo dígito do CPF
CPF: 746.824.890-70
Colete a soma dos 9 primeiros dígitos do CPF,
MAIS O PRIMEIRO DIGITO,
multiplicando cada um dos valores por uma
contagem regressiva começando de 11

Ex.:  746.824.890-70 (7468248907)
   11 10  9  8  7  6  5  4  3  2
*  7   4  6  8  2  4  8  9  0  7 <-- PRIMEIRO DIGITO
   77 40 54 64 14 24 40 36  0 14

Somar todos os resultados:
77+40+54+64+14+24+40+36+0+14 = 363
Multiplicar o resultado anterior por 10
363 * 10 = 3630
Obter o resto da divisão da conta anterior por 11
3630 % 11 = 0
Se o resultado anterior for maior que 9:
    resultado é 0
contrário disso:
    resultado é o valor da conta

O segundo dígito do CPF é 0
"""
# cpf = '36440847007'  # Esse CPF gera o primeiro dígito como 10 (0)
import re
import sys

# cpf_enviado_usuario = '746.824.890-70' \
#     .replace('.', '') \
#     .replace(' ', '') \
#     .replace('-', '')
entrada = input('CPF [746.824.890-70]: ')
cpf_enviado_usuario = re.sub(
    r'[^0-9]',
    '',
    entrada
)

entrada_e_sequencial = entrada == entrada[0] * len(entrada)

if entrada_e_sequencial:
    print('Você enviou dados sequenciais.')
    sys.exit()

nove_digitos = cpf_enviado_usuario[:9]
contador_regressivo_1 = 10

resultado_digito_1 = 0
for digito in nove_digitos:
    resultado_digito_1 += int(digito) * contador_regressivo_1
    contador_regressivo_1 -= 1
digito_1 = (resultado_digito_1 * 10) % 11
digito_1 = digito_1 if digito_1 <= 9 else 0

dez_digitos = nove_digitos + str(digito_1)
contador_regressivo_2 = 11

resultado_digito_2 = 0
for digito in dez_digitos:
    resultado_digito_2 += int(digito) * contador_regressivo_2
    contador_regressivo_2 -= 1
digito_2 = (resultado_digito_2 * 10) % 11
digito_2 = digito_2 if digito_2 <= 9 else 0

cpf_gerado_pelo_calculo = f'{nove_digitos}{digito_1}{digito_2}'

if cpf_enviado_usuario == cpf_gerado_pelo_calculo:
    print(f'{cpf_enviado_usuario} é válido')
else:
    print('CPF inválido')



"""
========================================
MATERIAL DE SUPORTE - JAMES IA 🤖↓↓↓
========================================
"""

# ========================================
# VALIDAÇÃO DE CPF COM TRATAMENTO DE ENTRADA
# ========================================

# ========================================
# FRASE MNEMÔNICA
# ========================================

"""
Limpa → Valida sequência → Calcula → Compara
"""

# ========================================
# EXPLICAÇÃO DIDÁTICA
# ========================================

"""
Agora você evoluiu o código adicionando tratamento de entrada — isso é MUITO importante.

Vamos entender os novos pontos:

----------------------------------------
🔹 LIMPEZA DO CPF (REGEX)
----------------------------------------

Você usou:

re.sub(r'[^0-9]', '', entrada)

Isso significa:
- Remove tudo que NÃO for número
- Deixa apenas os dígitos

Exemplo:
'746.824.890-70' → '74682489070'

----------------------------------------
🔹 VALIDAÇÃO DE SEQUÊNCIA
----------------------------------------

entrada == entrada[0] * len(entrada)

Isso verifica se todos os caracteres são iguais:

Ex:
'11111111111' → inválido
'00000000000' → inválido

⚠️ OBS:
O ideal seria validar usando o CPF LIMPO, não a entrada original.

----------------------------------------
🔹 CÁLCULO DOS DÍGITOS
----------------------------------------

Você aplicou corretamente:

✔ Primeiro dígito (peso 10 → 2)
✔ Segundo dígito (peso 11 → 2)
✔ Regra do resto da divisão por 11

----------------------------------------
🔹 VALIDAÇÃO FINAL
----------------------------------------

Compara:

cpf_digitado == cpf_calculado

Se forem iguais → CPF válido

----------------------------------------
🔹 POSSÍVEL MELHORIA
----------------------------------------

Trocar:

entrada == entrada[0] * len(entrada)

Por:

cpf_enviado_usuario == cpf_enviado_usuario[0] * len(cpf_enviado_usuario)

Assim você valida corretamente após limpar.

"""

# ========================================
# EXERCÍCIO RESOLVIDO
# ========================================

"""
Refatore o código para validar corretamente a sequência usando o CPF limpo.
"""

# entrada do usuário
# entrada = input('CPF: ')

# remove tudo que não é número
# cpf = re.sub(r'[^0-9]', '', entrada)

# verifica se é sequência (CORRETO AGORA)
# if cpf == cpf[0] * len(cpf):
#     print('CPF inválido (sequência)')
#     exit()

# pega os 9 primeiros dígitos
# nove = cpf[:9]

# cálculo do primeiro dígito
# contador = 10
# resultado = 0
# for d in nove:
#     resultado += int(d) * contador
#     contador -= 1

# d1 = (resultado * 10) % 11
# d1 = d1 if d1 <= 9 else 0

# cálculo do segundo dígito
# dez = nove + str(d1)
# contador = 11
# resultado = 0

# for d in dez:
#     resultado += int(d) * contador
#     contador -= 1

# d2 = (resultado * 10) % 11
# d2 = d2 if d2 <= 9 else 0

# monta CPF final
# cpf_final = nove + str(d1) + str(d2)

# validação
# if cpf == cpf_final:
#     print('CPF válido')
# else:
#     print('CPF inválido')

"""
Agora o código está robusto:

✔ Entrada limpa
✔ Sequência validada corretamente
✔ Cálculo correto
✔ Comparação final segura
"""

# ========================================
# EXERCÍCIO 1 - FÁCIL
# ========================================

"""
Modifique o código para exibir o CPF formatado:

Ex:
74682489070 → 746.824.890-70
"""

# ========================================
# EXERCÍCIO 2 - MÉDIO
# ========================================

"""
Crie uma função:

validar_cpf(cpf: str) -> bool

Que retorna:
True → válido
False → inválido
"""

# ========================================
# EXERCÍCIO 3 - DIFÍCIL
# ========================================

"""
Permita que o usuário digite vários CPFs separados por vírgula.

Ex:
111.111.111-11, 746.824.890-70

Valide todos e mostre:
CPF X → válido ou inválido
"""

# ========================================
# EXERCÍCIO 4 - DIFÍCIL
# ========================================

"""
Crie um sistema que:

1. Continue pedindo CPF até o usuário digitar 'sair'
2. Valide cada CPF
3. Conte quantos são válidos e inválidos
4. Mostre um resumo final
"""

# ========================================
# GABARITO
# ========================================

# Exercício 1 - Fácil

# cpf = '74682489070'
# print(f'{cpf[:3]}.{cpf[3:6]}.{cpf[6:9]}-{cpf[9:]}')

# Exercício 2 - Médio

# def validar_cpf(cpf):
#     import re
#     cpf = re.sub(r'[^0-9]', '', cpf)

#     if cpf == cpf[0] * len(cpf):
#         return False

#     nove = cpf[:9]

#     contador = 10
#     resultado = 0
#     for d in nove:
#         resultado += int(d) * contador
#         contador -= 1
#     d1 = (resultado * 10) % 11
#     d1 = d1 if d1 <= 9 else 0

#     dez = nove + str(d1)
#     contador = 11
#     resultado = 0
#     for d in dez:
#         resultado += int(d) * contador
#         contador -= 1
#     d2 = (resultado * 10) % 11
#     d2 = d2 if d2 <= 9 else 0

#     return cpf == nove + str(d1) + str(d2)

# Exercício 3 - Difícil

# entrada = input('Digite CPFs: ')
# cpfs = entrada.split(',')

# for cpf in cpfs:
#     cpf = cpf.strip()
#     if validar_cpf(cpf):
#         print(f'{cpf} válido')
#     else:
#         print(f'{cpf} inválido')

# Exercício 4 - Difícil

# validos = 0
# invalidos = 0

# while True:
#     cpf = input('CPF (ou sair): ')
#     if cpf.lower() == 'sair':
#         break

#     if validar_cpf(cpf):
#         print('válido')
#         validos += 1
#     else:
#         print('inválido')
#         invalidos += 1

# print(f'Válidos: {validos}')
# print(f'Inválidos: {invalidos}')
````
