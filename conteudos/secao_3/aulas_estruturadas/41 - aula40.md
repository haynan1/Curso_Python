# Aula 41 - Calculadora com WHILE + Validação de Entrada + Try/Except

Fonte Python editável: `../41 - aula40.py`

Bloco: **Repetições**

## Navegação

- Aula anterior: [ITERANDO STRINGS COM WHILE EM PYTHON](40 - aula39.md)
- Próxima aula: [WHILE + ELSE EM PYTHON](42 - aula41.md)

## Ideia central

Enquanto o usuário quiser calcular,
o WHILE vai rodar.

Se o número der erro,
TRY tenta converter
e EXCEPT vai tratar.

Se o operador for válido,
a conta vai funcionar.

Se quiser sair,
BREAK vai encerrar.

## Conteúdo estruturado

### Explicação

Este programa implementa uma calculadora simples usando WHILE.

A estrutura principal é:

while True

Isso cria um LOOP INFINITO. O programa continuará executando
até que o usuário escolha sair.

Dentro do loop acontecem várias etapas importantes:

1) Entrada de dados
O usuário digita dois números e um operador matemático.

2) Conversão de tipo
Os números digitados são strings. Para realizar cálculos,
precisamos convertê-los para float.

Isso é feito com:

float(valor)

Mas se o usuário digitar algo inválido (ex: letras),
isso gera um erro.

3) Tratamento de erro (Try / Except)

try:
    tenta executar o código

except:
    executa se ocorrer erro

Isso evita que o programa quebre.

4) Validação do operador

A variável:

operadores_permitidos = "+-/*"

define quais operadores são aceitos.

Depois verificamos:

if operador not in operadores_permitidos

Se o operador não estiver na lista,
o programa pede novamente.

5) Execução da operação

Usamos IF / ELIF para verificar qual operador foi digitado.

Cada operador executa uma operação matemática diferente.

6) Pergunta se o usuário quer sair

O código:

input(...).lower().startswith("s")

faz três coisas:

lower() → transforma em minúsculo
startswith("s") → verifica se começa com "s"

Exemplo:
"s"
"sim"
"sair"

Todos retornam True.

7) Encerramento do programa

Se o usuário quiser sair:

break

O break encerra o WHILE.

### Divisões detectadas no arquivo

- TÍTULO DO CONTEÚDO
- FRASE MNEMÔNICA
- EXPLICAÇÃO DIDÁTICA
- EXERCÍCIO RESOLVIDO
- EXERCÍCIO 1 - FÁCIL
- EXERCÍCIO 2 - MÉDIO
- EXERCÍCIO 3 - DIFÍCIL
- EXERCÍCIO 4 - DIFÍCIL
- GABARITO
- Exercício 1 - Fácil
- Exercício 2 - Médio
- Exercício 3 - Difícil
- Exercício 4 - Difícil

## Como estudar esta aula

1. Leia a explicação.
2. Abra o arquivo Python fonte.
3. Execute o código.
4. Altere pequenos trechos.
5. Resolva os exercícios presentes no arquivo.
6. Registre dúvidas no Obsidian.

## Código fonte completo

````python
''' Calculadora com while'''

while True:
    numero_1 = input("Digite um número: ")
    numero_2 = input("Digite outro número: ")
    operador = input("Digite o operador (+-/*): ")

    numeros_validos = None
    num_1_float = 0
    num_2_float = 0

    try:
        num_1_float = float(numero_1)
        num_2_float = float(numero_2)
        numeros_validos = True
    except:
        numeros_validos = None


    if numeros_validos is None:
        print("Um ou ambos os números digitados são invalidados")
        continue


    operaores_permitidos = "+-/*"


    if operador not in operaores_permitidos:
        print("Operador inválido.")
        continue

    if len(operador) > 1:
        print("Digite apenas um operador.")
        continue

 
    print("Realizando sua conta. Confira o resultado abaixo: ")
    if operador == "+":
        print(f"{num_1_float} + {num_2_float} =", num_1_float + num_2_float)
    elif operador == "-": 
        print(f"{num_1_float} + {num_2_float} =", num_1_float - num_2_float)
    elif operador == "/": 
        print(f"{num_1_float} + {num_2_float} =", num_1_float / num_2_float)
    elif operador == "*": 
        print(f"{num_1_float} + {num_2_float} =", num_1_float * num_2_float)
    else:
        print("Nunca deveria chegar aqui.")


    sair = input("Quer sair? [s]im: ").lower().startswith("s")

    if sair is True:
        break        




"""
========================================
MATERIAL DE SUPORTE - JAMES IA 🤖↓↓↓
========================================
"""

# ========================================
# TÍTULO DO CONTEÚDO
# ========================================

# Calculadora com WHILE + Validação de Entrada + Try/Except

# ========================================
# FRASE MNEMÔNICA
# ========================================

"""
Enquanto o usuário quiser calcular,
o WHILE vai rodar.

Se o número der erro,
TRY tenta converter
e EXCEPT vai tratar.

Se o operador for válido,
a conta vai funcionar.

Se quiser sair,
BREAK vai encerrar.
"""

# ========================================
# EXPLICAÇÃO DIDÁTICA
# ========================================

"""
Este programa implementa uma calculadora simples usando WHILE.

A estrutura principal é:

while True

Isso cria um LOOP INFINITO. O programa continuará executando
até que o usuário escolha sair.

Dentro do loop acontecem várias etapas importantes:

1) Entrada de dados
O usuário digita dois números e um operador matemático.

2) Conversão de tipo
Os números digitados são strings. Para realizar cálculos,
precisamos convertê-los para float.

Isso é feito com:

float(valor)

Mas se o usuário digitar algo inválido (ex: letras),
isso gera um erro.

3) Tratamento de erro (Try / Except)

try:
    tenta executar o código

except:
    executa se ocorrer erro

Isso evita que o programa quebre.

4) Validação do operador

A variável:

operadores_permitidos = "+-/*"

define quais operadores são aceitos.

Depois verificamos:

if operador not in operadores_permitidos

Se o operador não estiver na lista,
o programa pede novamente.

5) Execução da operação

Usamos IF / ELIF para verificar qual operador foi digitado.

Cada operador executa uma operação matemática diferente.

6) Pergunta se o usuário quer sair

O código:

input(...).lower().startswith("s")

faz três coisas:

lower() → transforma em minúsculo
startswith("s") → verifica se começa com "s"

Exemplo:
"s"
"sim"
"sair"

Todos retornam True.

7) Encerramento do programa

Se o usuário quiser sair:

break

O break encerra o WHILE.
"""

# ========================================
# EXERCÍCIO RESOLVIDO
# ========================================

"""
Crie uma calculadora que:

1) Peça dois números
2) Peça um operador (+ - * /)
3) Valide se os números são válidos
4) Execute o cálculo
5) Pergunte se o usuário deseja sair
"""

# Loop infinito
# while True:

#     # Entrada dos números
#     numero_1 = input("Digite um número: ")
#     numero_2 = input("Digite outro número: ")

#     # Entrada do operador
#     operador = input("Digite o operador (+ - * /): ")

#     # Variáveis auxiliares
#     numeros_validos = None
#     num1 = 0
#     num2 = 0

#     # Tentativa de conversão
#     try:
#         num1 = float(numero_1)
#         num2 = float(numero_2)
#         numeros_validos = True
#     except:
#         numeros_validos = None

#     # Se números inválidos
#     if numeros_validos is None:
#         print("Número inválido")
#         continue

#     # Operadores permitidos
#     operadores = "+-/*"

#     # Verifica operador
#     if operador not in operadores:
#         print("Operador inválido")
#         continue

#     # Executa operação
#     if operador == "+":
#         print(num1 + num2)

#     elif operador == "-":
#         print(num1 - num2)

#     elif operador == "*":
#         print(num1 * num2)

#     elif operador == "/":
#         print(num1 / num2)

#     # Pergunta se deseja sair
#     sair = input("Quer sair? [s/n]: ").lower().startswith("s")

#     # Encerra o programa
#     if sair:
#         break


"""
Explicação do código resolvido.

O WHILE mantém o programa rodando continuamente.

O TRY tenta converter os números para float.
Se falhar, o EXCEPT impede que o programa quebre.

O CONTINUE faz o loop reiniciar imediatamente.

Os IF/ELIF verificam qual operador foi digitado.

O BREAK encerra o loop quando o usuário decide sair.
"""

# ========================================
# EXERCÍCIO 1 - FÁCIL
# ========================================

"""
Crie um programa que:

1) Peça dois números ao usuário
2) Converta para float usando try/except
3) Some os dois números
4) Mostre o resultado
"""

# ========================================
# EXERCÍCIO 2 - MÉDIO
# ========================================

"""
Crie uma calculadora que:

1) Peça dois números
2) Peça um operador (+ ou -)
3) Execute a operação correta
4) Se o operador for inválido, informe o erro
"""

# ========================================
# EXERCÍCIO 3 - DIFÍCIL
# ========================================

"""
Crie uma calculadora com WHILE que:

1) Permita operações + - * /
2) Valide números com try/except
3) Continue rodando até o usuário digitar "sair"
"""

# ========================================
# EXERCÍCIO 4 - DIFÍCIL
# ========================================

"""
Crie uma calculadora mais robusta que:

1) Permita várias operações seguidas
2) Não quebre se o usuário digitar letras
3) Não aceite operadores inválidos
4) Pergunte após cada operação se deseja continuar
"""

# ========================================
# GABARITO
# ========================================

# Exercício 1 - Fácil

# numero1 = input("Digite um número: ")
# numero2 = input("Digite outro número: ")

# try:
#     num1 = float(numero1)
#     num2 = float(numero2)

#     resultado = num1 + num2

#     print("Resultado:", resultado)

# except:
#     print("Número inválido")


# Exercício 2 - Médio

# numero1 = float(input("Digite um número: "))
# numero2 = float(input("Digite outro número: "))
# operador = input("Digite + ou - : ")

# if operador == "+":
#     print(numero1 + numero2)

# elif operador == "-":
#     print(numero1 - numero2)

# else:
#     print("Operador inválido")


# Exercício 3 - Difícil

# while True:

#     numero1 = input("Número 1: ")
#     numero2 = input("Número 2: ")

#     try:
#         num1 = float(numero1)
#         num2 = float(numero2)
#     except:
#         print("Número inválido")
#         continue

#     operador = input("Operador (+ - * /): ")

#     if operador == "+":
#         print(num1 + num2)

#     elif operador == "-":
#         print(num1 - num2)

#     elif operador == "*":
#         print(num1 * num2)

#     elif operador == "/":
#         print(num1 / num2)

#     else:
#         print("Operador inválido")
#         continue

#     sair = input("Digite sair para encerrar: ")

#     if sair == "sair":
#         break


# Exercício 4 - Difícil

# while True:

#     numero1 = input("Número 1: ")
#     numero2 = input("Número 2: ")

#     try:
#         num1 = float(numero1)
#         num2 = float(numero2)
#     except:
#         print("Número inválido")
#         continue

#     operador = input("Operador (+ - * /): ")

#     operadores = "+-/*"

#     if operador not in operadores:
#         print("Operador inválido")
#         continue

#     if operador == "+":
#         resultado = num1 + num2

#     elif operador == "-":
#         resultado = num1 - num2

#     elif operador == "*":
#         resultado = num1 * num2

#     elif operador == "/":
#         resultado = num1 / num2

#     print("Resultado:", resultado)

#     continuar = input("Deseja continuar? (s/n): ")

#     if continuar.lower().startswith("n"):
#         break
````
